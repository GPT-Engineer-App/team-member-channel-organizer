import { Box, Heading, Text } from "@chakra-ui/react";

const Member = () => {
  return (
    <Box p={5}>
      <Heading mb={4}>Member Details</Heading>
      <Text fontSize="lg">View and manage individual member details and tasks.</Text>
    </Box>
  );
};

export default Member;