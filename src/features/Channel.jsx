import { Box, Heading, Text } from "@chakra-ui/react";

const Channel = () => {
  return (
    <Box p={5}>
      <Heading mb={4}>Channel Communications</Heading>
      <Text fontSize="lg">Engage in discussions, share files, and collaborate in real-time.</Text>
    </Box>
  );
};

export default Channel;